#include <stdio.h>

/**
 *main - function
 *
 *Return: valve 0
 */

int main(void)
{
	int b;

	for (a = 0; a < 10; b++)
	{
		putchar(a + '0');
	}
	for (a = 'a'; a <= 'f'; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);

}
