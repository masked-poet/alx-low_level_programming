#include <stdio.h>

/**
 *main - function
 *
 *Return: valve 0
 */

int main(void)
{
	int b;

	for (b = 0; a < 10; b++)
	{
		putchar(b + '0');
	}
	for (b = 'b'; b <= 'f'; b++)
	{
		putchar(b);
	}
	putchar('\n');
	return (0);

}
